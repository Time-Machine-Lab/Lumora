{
  "name": "@lumora/mock-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Lumora 示例插件：演示 panel / command / toolbar / assetLoader / aiProvider / exporter 六类贡献项",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./lumora.plugin.json": "./lumora.plugin.json"
  },
  "files": ["dist", "lumora.plugin.json"],
  "scripts": {
    "build": "vite build && tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@lumora/plugin-sdk": "0.1.0"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "react": "^19.0.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
